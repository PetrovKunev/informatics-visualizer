{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build",
  "outputDirectory": ".vercel/output",
  "framework": "nextjs",
  "regions": ["fra1"],
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
